//let numbers = [1, 2, 3, 4, 5];

function includes(arr, number) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === number) {
            return true
        }

    }

    return false;
}

















//let arr1 = includes(numbers, 20);
//console.log(arr1);
