//let numbers = [1, 2, 3, 4, 5];

function reverseArray(arr){
    if (!Array.isArray(arr)) {
        return null;
    }
   


   return arr.reverse(); 
}






















//let arr = reverseArrey(numbers);
//console.log(arr);