//let numbers = [1, 2, 3, 4, 5];

function cloneArr(arr){
    if (!Array.isArray(arr)) {
        return null;
    }
   


    return arr.slice(0);

}







//let arr1 = cloneArr(numbers);
//console.log(arr1);